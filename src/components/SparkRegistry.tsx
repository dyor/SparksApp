import { BaseSpark } from '../types/spark';

// Re-export everything from sparkRegistryData to maintain backward compatibility
export { sparkRegistry, getSparkById, getAllSparks } from './sparkRegistryData';
